var a = 0;
var temp;

for (var b = 1; a < 10000; b = temp + b) {
	PrintLine(a);
	temp = a;
	a = b;
}

fn SayHi(firstName, lastName) {
	PrintLine("Hi, " + firstName + " " + lastName + "!");
}

SayHi("Dear", "Reader");